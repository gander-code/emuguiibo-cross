<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="emuGUIibo_Cross.Views.AmiiboSelectionView">
  <Grid ColumnDefinitions="250,250,1.5*" RowDefinitions="Auto,Auto" Margin="4">
    <ListBox
      Margin="10"
      Items="{Binding AmiiboSeries}"
      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
      Grid.Row="0" Grid.Column="0" Grid.RowSpan="2">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Border CornerRadius="4" Padding="4">
            <TextBlock Text="{Binding}" />
          </Border>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
    <ListBox
      Margin="10"
      Items="{Binding AmiibosInSeries}"
      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
      Grid.Row="0" Grid.Column="1" Grid.RowSpan="2">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Border CornerRadius="4" Padding="4">
            <TextBlock Text="{Binding}" />
          </Border>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
